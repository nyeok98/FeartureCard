export const Title = () => {
  return (
    <h1
      style={{
        width: "100%",
        paddingLeft: "calc(var(--gap) * 3)",
        marginBottom: "var(--gap)",
        textAlign: "left",
        fontFamily: "YouAndI, sans-serif",
        fontSize: "var(--fs-lg)",
      }}
    >
      스크롤링 라이브러리
    </h1>
  );
};
